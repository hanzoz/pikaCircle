name: pikacircle
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  liquid_glass_widgets: ^0.17.1

  # Backend: Appwrite (Account, TablesDB, Functions, Storage).
  # PINNED to 20.3.3: the self-hosted server is Appwrite 1.8.1. The File model
  # gained `encryption`/`compression` fields on the SERVER in 1.9.0. Flutter SDK
  # 21.0.0+ added those as NON-NULLABLE fields to its File model while still
  # targeting response format 1.8.0, so `Storage.createFile` (and getFile/
  # listFiles) throw `type 'Null' is not a subtype of type 'bool'` when parsing
  # a 1.8.1 response that omits `encryption`. 20.3.3 is the last SDK whose File
  # model matches a 1.8.x server. Do not bump past 20.x until the server is
  # upgraded to 1.9.x (then move to SDK 23.x).
  appwrite: 20.3.3

  # HTTP client for direct REST API calls (file uploads).
  http: ^1.2.0

  # Google OAuth: opens the Appwrite OAuth2 token URL in a browser tab and
  # captures the deep-link callback (userId/secret). Promoted to a direct
  # dependency because the auth data source imports it explicitly.
  flutter_web_auth_2: ^5.0.2

  # State management + dependency injection.
  # Held to the riverpod 3.2.1 core so flutter_riverpod and riverpod_annotation
  # (required by riverpod_generator 4.0.3) resolve together under the current
  # Flutter SDK analyzer/meta pins.
  flutter_riverpod: 3.3.1
  riverpod_annotation: 4.0.2

  # Declarative routing with auth-redirect guard.
  go_router: ^17.3.0

  # Swipeable discovery cards UI.
  flutter_card_swiper: ^7.0.2

  # Loads .env.client (public Appwrite endpoint/project values only).
  flutter_dotenv: ^6.0.1

  # Reads the installed app version/build number for settings screens.
  package_info_plus: ^8.0.3

  # Lets users choose avatar photos from gallery or camera.
  image_picker: ^1.2.0

  # Functional error handling (Either/Result) for the data/domain layers.
  fpdart: ^1.2.0

  # On-device key/value storage for the profile cache (plain Hive + manual
  # JSON serialization; no TypeAdapters/codegen).
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Immutable models / entities and JSON (de)serialization.
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  share_plus: ^12.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation for Riverpod providers, Freezed models, and JSON.
  # Versions are held to the analyzer ^9 line so they stay compatible with the
  # meta 1.17.0 pin from the current Flutter SDK (flutter_test).
  build_runner: ^2.15.0
  riverpod_generator: 4.0.3
  freezed: 3.2.5
  json_serializable: 6.13.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Public client configuration. Only .env.client is bundled; the root .env
  # holds the server API key and must never ship in the app.
  assets:
    - .env.client

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
